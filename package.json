{
  "name": "mini-video-platform",
  "version": "1.0.0",
  "description": "A mini video platform where you catalog YouTube videos, comment, and like them — backed by a real SQLite database.",
  "main": "server.js",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
